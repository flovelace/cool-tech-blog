{{#if posts.length}}
<ul class="post-list flex-column">
    {{#each posts as |post|}}
    <li class="post-list-item">
        {{> post-info post}}
    </li>
    {{/each}}
</ul>
{{else}}
<h2>Write a cool tech blog <a href="/dashboard" class="no-posts">post?</a>?</h2>
{{/if}}